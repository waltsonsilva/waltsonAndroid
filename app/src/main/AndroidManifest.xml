<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.walts.projetowidgets">



    <uses-feature android:name="android.hardware.Camera" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".nativo.CallActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".nativo.SharedActivity"></activity> -->
        <!-- <activity android:name=".nativo.CamActivity"></activity> -->
        <!-- <activity android:name=".nativo.CallActivity"></activity> -->
        <!-- <activity android:name=".FrameLayout"></activity> -->
        <!-- <activity android:name=".LayoutsActivity"></activity> -->
        <!-- <activity android:name=".LinearActivity"></activity> -->
        <!-- <activity android:name=".TableActiviy"></activity> -->
        <activity android:name=".layouts.GridLayout"></activity>

    </application>

</manifest>